extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div
      h1 Hey, that's me!
      p.lead.text-light
        | Several projects can be found in the gallery below. With every project you have the oppertunity to expand the knowledge.
        | Click on a project to display further informations. The information side also contains links to GitHub and the live view.
  article.grid-container.portfolio-index
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3
      each project in projects
        .cell
          a(href=`project/${project.id}`)
            img.thumbnail(src=`${project.image_urls[0]}`)
            h5= project.project_name
      
